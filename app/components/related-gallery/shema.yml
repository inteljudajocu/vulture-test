_description:
  Different gallery photo component

gallery:
  _has:
    input: complex-list
    props:
      -
        prop: image
        _label: imageUrl
        _has:
          input: text
          type: url

_groups:
  settings:
    fields:
    - gallery